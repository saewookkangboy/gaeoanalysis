# npm 설정 파일
# legacy-peer-deps 설정
legacy-peer-deps=false

# 참고: npm warn config production 경고는 Vercel/Railway가 
# 내부적으로 --production 플래그를 사용할 때 발생하는 것으로,
# 실제 문제는 아니며 무시해도 됩니다.
# 최신 npm에서는 --omit=dev를 권장하지만, 
# 배포 플랫폼의 호환성을 위해 --production을 계속 사용합니다.

